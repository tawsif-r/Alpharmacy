apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
data:
  POSTGRES_USER=tawsif
  POSTGRES_PASSWORD=123
  POSTGRES_DB=pharmacy_db
  POSTGRES_HOST=db
  POSTGRES_PORT=5432
  DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
  # Add other .env keys here
